// Resum js de Platzi i Freecodecamp 
// url : https://www.youtube.com/watch?v=zBk-Hyv6ykw

// Js és el llenguatge més popular, bàsicament perquè és l'únic llenguatge de programació que corre en el navegador.

// Principals mètodes agefir i eliminar elements en Arrays:
// .push() -> col·locar element al final de l'array
// .pop() -> eliminar l'últim element de l'array
// .shift() -> eliminar el primer element de l'array
// .unshift() -> agregar element al principi de l'array

// JSON.stringify() -> millor manera de visualitzar un array a la consola:
  // Així es visualitza console.log(array) -> (2) ['pera', 'poma']
  // Així es visualitza console.log(JSON.stringify(array)) -> ["pera","poma"]

// .hasOwnProperty() -> Per verificar si l'objecte té una propietat especifica -> retorna true or false
// const {x, y, z} = coordenades -> 
  // console.log(x)
  // Amb la desestructuració asignem cada valor a la seva variable corresponent.

// Les classes serveixen per crear objectes. Són molt útils per evitar repetir codi i per crear una plantilla general de l'estructura d'un tipus d'objecte.